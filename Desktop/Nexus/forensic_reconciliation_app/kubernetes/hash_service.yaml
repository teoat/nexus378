apiVersion: apps/v1
kind: Deployment
metadata:
  name: hash-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hash-service
  template:
    metadata:
      labels:
        app: hash-service
    spec:
      containers:
      - name: hash-service
        image: hash-service:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: hash-service
spec:
  selector:
    app: hash-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP
